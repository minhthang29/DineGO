namespace Core.Constant
{
    /// <summary>
    /// Provides constant format strings used throughout the system.
    /// Helps avoid typos and makes it easier to maintain or refactor date/time and other format patterns.
    /// Use FormatConstants.[NAME] instead of hard-coded format strings in your code.
    /// </summary>
    /// <author>Phuonghh</author>
    public static class FormatConstants
    {
        public const string YYYY_MM_DD_HH_MM = "yyyy-MM-dd HH:mm";
    }
}